{% extends 'GoFlashBundle::frontLayout.html.twig' %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/joueuredit.css') }}">
{% endblock %}
{% block main %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12 text-center">
                <div class="col-lg-12">
                    <h1>La partie est maintenant démarrée</h1>
                </div>
            {{ form_start(edit_form) }}
                    {{ form_errors(edit_form) }}
                <div class="col-lg-12">
                    {{ form_widget(edit_form) }}
                </div>
                <div class="col-lg-12">
                    <button id="sendPic" type="submit"><i class="fa fa-play fa-2x"></i></button>
                </div>
            {{ form_end(edit_form) }}
            </div>
        </div>
    </div>
{% endblock %}